package service;

import result.PeopleResult;

public class FindPersonIDService {
    /**
     *
     * Searches the Person table and returns the Person with the correct ID.
     *
     * @return the single Person object with the specified ID
     */
    PeopleResult find() {
        return null;
    }
}
